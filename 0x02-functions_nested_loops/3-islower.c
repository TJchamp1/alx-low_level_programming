#include "main.h"
/**
* _islower - function that returns the lower
* @c: will be used
* Return - it returns 0 or 1
*/

int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
{
return (0);
}
}
