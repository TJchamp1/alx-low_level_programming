#include "main.h"
/**
* print_alphabet - function that prints alphabets
* adsf
* Return: @n the value of the last digit
*/

int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
{
return (0);
}
}
