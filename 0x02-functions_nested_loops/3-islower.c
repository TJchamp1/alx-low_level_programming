#include "main.h"
/**
* _islower - function that returns the lower
* adsf
* Return: @i and @1 or @0  the value of the last digit
*/

int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
{
return (0);
}
}
